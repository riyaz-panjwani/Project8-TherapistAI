# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
env/
.python-version

# Database (local only — each user gets their own)
backend/memory/therapist.db

# Model checkpoints (large — train locally)
training/checkpoints/

# GloVe vectors (large — run download_glove.py)
training/data/glove*
training/data/*.zip

# Env / secrets
.env
*.env

# macOS
.DS_Store

# Logs
*.log
